# SimpleLogin-SpringBoot with login & registration features

Works in tandem with SimpleLogin-reactJs to provide frontend web pages.
MySQL is used as the database to store the necessary data.
Application relies entirely on localhost, no servers needed.


